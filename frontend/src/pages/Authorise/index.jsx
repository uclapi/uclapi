import '../../lib/ErrorReporting'

import React from 'react'
import ReactDOM from 'react-dom'
import Authorise from './Authorise.jsx'

ReactDOM.render(
  <Authorise />,
  document.querySelector(`.app`)
)
