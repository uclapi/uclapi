name: CI/CD

on:
  push

jobs:
  build:
    name: build and test
    runs-on: ubuntu-latest
    container: ubuntu:20.04 # necessary because of https://github.community/t/services-name-resolution-error/16716/4
    environment: Staging
    services:
      redis:
        image: redis
        options: >-
          --health-cmd "redis-cli ping"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
      postgres:
        image: postgres
        env:
          POSTGRES_PASSWORD: supersecure
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
    steps:
      - name: setup python
        uses: actions/setup-python@v2
        with:
          python-version: '3.7'

      - name: setup nodejs
        uses: actions/setup-node@v2
        with:
          node-version: '14'

      - name: checkout code
        uses: actions/checkout@v2

      - name: setup oracle
        run: ./.github/workflows/scripts/setup-oracle.sh

      - name: setup python dependencies
        run: |
          cd ./backend/uclapi && \
          which pip && which pip3 && \
          pip install -r requirements.txt && \
          pip install codecov && \
          pip install $(cat requirements.txt | grep "cx-Oracle")
      
      - name: setup frontend dependencies
        run: |
          ./backend/uclapi/webpack-stats.sample.json ./backend/uclapi/static/webpack-stats.json && \
          cd ./frontend/ && \
          npm ci

      - name: build frontend
        run: |
          cd ./frontend && \
          npm run build
      
      - name: run migrations
        env: # Or as an environment variable
          DB_UCLAPI_HOST: postgres
          DB_UCLAPI_PASSWORD: supersecure
          OCCUPEYE_BASE_URL: ${{ secrets.OCCUPEYE_BASE_URL }}
          OCCUPEYE_DEPLOYMENT_ID: ${{ secrets.OCCUPEYE_DEPLOYMENT_ID }}
          OCCUPEYE_DEPLOYMENT_NAME: ${{ secrets.OCCUPEYE_DEPLOYMENT_NAME }}
          OCCUPEYE_PASSWORD: ${{ secrets.OCCUPEYE_PASSWORD }}
          OCCUPEYE_THREAD_LIMIT: ${{ secrets.OCCUPEYE_THREAD_LIMIT }}
          OCCUPEYE_USERNAME: ${{ secrets.OCCUPEYE_USERNAME }}
          REDIS_UCLAPI_HOST: ${{ secrets.REDIS_UCLAPI_HOST }}
          SECRET_KEY: ${{ secrets.SECRET_KEY }}
          SHIB_TEST_USER: ${{ secrets.SHIB_TEST_USER }}
          UCLAPI_PRODUCTION: False
          UCLAPI_RUNNING_ON_AWS_ELB: False
        run: |
          cd ./backend/uclapi && \
          python ./manage.py migrate

      - name: run our tests and check test coverage
        run: |
          coverage run --source='.' --omit='*migrations*' manage.py test --testrunner && \
          'uclapi.custom_test_runner.NoDbTestRunner' --settings=uclapi.settings_mocked && \
          codecov
